/**
 * Limites de consultas publicas (mapa, listados). Centralizados para escalar sin
 * cambiar comportamiento en cada componente por separado.
 */

/** Marcadores maximos por tipo (taller / tienda) en el mapa de la landing. */
export const MAPA_MAX_PUNTOS_POR_TIPO = 350;

/** Talleres por pagina en busqueda por estado / ciudad / especialidad. */
export const PAGE_SIZE_TALLERES_BUSQUEDA = 40;